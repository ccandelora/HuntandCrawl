#if canImport(XCTest)
import XCTest
@testable import HuntandCrawl

class HuntDetailViewTests: XCTestCase {
    
    func testBasic() {
        // Simple test that always passes
        XCTAssertTrue(true)
    }
}
#endif 